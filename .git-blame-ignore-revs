# Commits that only moved code around, never changed what it does.
# `git blame` skips them so it points at the commit that wrote the logic.
#
# Enable locally with:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
# GitHub applies this file automatically.

# style: apply Allman braces and 4-space indent across the codebase
7c0221dba5df546a7384c4af28014490ffe41e09
